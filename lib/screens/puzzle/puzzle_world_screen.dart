import 'package:flutter/material.dart';

class PuzzleWorldScreen extends StatelessWidget {
  const PuzzleWorldScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.greenAccent,
    );
  }
}
